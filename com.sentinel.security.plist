<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
    "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    Sentinel LaunchAgent
    Install: cp com.sentinel.security.plist ~/Library/LaunchAgents/
             launchctl load ~/Library/LaunchAgents/com.sentinel.security.plist

    Uninstall: launchctl unload ~/Library/LaunchAgents/com.sentinel.security.plist
               rm ~/Library/LaunchAgents/com.sentinel.security.plist
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.sentinel.security</string>

    <key>ProgramArguments</key>
    <array>
        <string>/usr/local/bin/sentinel</string>
    </array>

    <!-- Start at login -->
    <key>RunAtLoad</key>
    <true/>

    <!-- Restart if it crashes -->
    <key>KeepAlive</key>
    <true/>

    <!-- Wait 5s before restart after crash -->
    <key>ThrottleInterval</key>
    <integer>5</integer>

    <key>StandardOutPath</key>
    <string>/Users/Shared/sentinel-stdout.log</string>

    <key>StandardErrorPath</key>
    <string>/Users/Shared/sentinel-stderr.log</string>

    <!-- Low priority to not impact system performance -->
    <key>ProcessType</key>
    <string>Background</string>

    <key>Nice</key>
    <integer>10</integer>
</dict>
</plist>
